<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Màu hiệu ứng ripple -->
    <item android:id="@android:id/mask" android:drawable="@android:color/white"/> <!-- Màu mask -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/A3A798"/> <!-- Màu nền của CardView -->
            <corners android:radius="12dp"/> <!-- Bo góc cho CardView -->
        </shape>
    </item>
</ripple>
